Now update the related notes and todo description to reflect the algorithmic approach:

Updated the plan to replace the static color map with a deterministic hash-based algorithm that generates colors from the type string using HSL color space. New/unknown types automatically get distinct colors with no code changes needed.