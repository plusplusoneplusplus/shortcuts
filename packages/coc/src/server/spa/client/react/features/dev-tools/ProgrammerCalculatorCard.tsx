/**
 * ProgrammerCalculatorCard — placeholder body.
 *
 * The expression evaluator, DEC/HEX/OCT/BIN readouts, bit grid and width /
 * signedness controls land in the next slice. The card is registered now so the
 * panel shell has its default-expanded tool.
 */

export function ProgrammerCalculatorCard() {
    return (
        <p className="text-xs text-[#656d76] dark:text-[#999]">
            Expression evaluator, base readouts and the bit grid are coming next.
        </p>
    );
}
