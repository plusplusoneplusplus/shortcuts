{
  "name": "@plusplusoneplusplus/pipeline-cli",
  "version": "1.0.0",
  "description": "CLI tool for executing YAML-based AI pipelines",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "pipeline": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build:client": "node scripts/build-client.mjs",
    "build": "npm run build:client && tsc",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "eslint src"
  },
  "dependencies": {
    "@plusplusoneplusplus/pipeline-core": "1.0.0",
    "commander": "^12.1.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@types/node": "^20.0.0",
    "@types/js-yaml": "^4.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
