[package]
name = "coc-native-core"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
description = "Native primitives for the CoC server: one self-contained module per capability, starting with a generic repo file index"

[dependencies]
ignore = "0.4"
rayon = "1"

[dev-dependencies]
tempfile = "3"
