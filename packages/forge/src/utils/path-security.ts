/**
 * Directory containment check — Node.js only.
 *
 * Compatibility entry point; the implementation lives in
 * `@plusplusoneplusplus/coc-agent-sdk/platform`.
 */

export { isWithinDirectory } from '@plusplusoneplusplus/coc-agent-sdk/platform';
