/**
 * Process execution helpers — Node.js only.
 *
 * Compatibility entry point; the implementation lives in
 * `@plusplusoneplusplus/coc-agent-sdk/platform`.
 */

export { execAsync, execFileAsync } from '@plusplusoneplusplus/coc-agent-sdk/platform';
