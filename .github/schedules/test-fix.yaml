name: Test fix
cron: 0 */12 * * *
target: >-
  Fix any failing tests. If failures stem from recent changes, review related commits to determine intent: if behavior
  changed intentionally, update tests; otherwise fix the code. Avoid introducing new regressions.

  You must commit after the fix
params:
  pipeline: ''
onFailure: notify
outputFolder: ~/.coc/repos/ws-kss6a7/tasks
