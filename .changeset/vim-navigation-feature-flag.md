---
"@plusplusoneplusplus/coc": patch
---

Gate vim-style navigation (hjkl/jk/gg/G/Esc/i) in the SPA chat panes behind a new `vimNavigation.enabled` config flag (default off). The flag can be toggled at runtime from the Admin Panel Features card.
