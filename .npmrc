; Resolve @openai packages from public npm. Mirrored/proxied registries can lag
; behind new releases (e.g. @openai/codex-sdk), causing 404s during install.
@openai:registry=https://registry.npmjs.org/
